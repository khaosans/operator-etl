# Global skips only for rules that are noise everywhere.
# Site-specific false positives use `# nosec Bxxx` on the line (see docs/SECURITY-HARDENING.md).
skips:
  - B101  # assert used in tests and contracts — acceptable

exclude_dirs:
  - tests
  - .venv
  - harness
