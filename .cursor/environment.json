{
  "name": "operator-etl",
  "install": "./scripts/cloud-agent-install.sh",
  "terminals": [
    {
      "name": "dashboard",
      "command": "export PATH=\"$HOME/.local/bin:$PATH\"; uv run streamlit run dashboard/app.py --server.port 8501 --server.address 0.0.0.0"
    }
  ]
}
